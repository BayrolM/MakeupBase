/**
 * Configuración global del sistema — fuente única de verdad para el backend.
 * El frontend lee estos mismos valores desde src/lib/constants.ts.
 * Si cambias COSTO_ENVIO aquí, actualiza también el frontend (y viceversa).
 */
export const CONFIG = {
  COSTO_ENVIO: 8000,
  MONEDA: "COP",
};
